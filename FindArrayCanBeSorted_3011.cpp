#include <iostream>
#include <vector>

class FindArrayCanBeSorted_3011
{
private:
    /* data */
public:
    bool canSortArray(std::vector<int> &nums)
    {
        for (int i = 0; i < nums.size(); i++)
        {
            /* code */
        }
    }
};
int main()
{
    std::vector<int> nums = {8, 4, 2, 30, 15};
    FindArrayCanBeSorted_3011 f1;
    f1.canSortArray(nums);
    return 0;
}