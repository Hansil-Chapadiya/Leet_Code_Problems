#include <iostream>
#include <vector>
#include <deque>
class ShortestSubArrayAtLeastK_862
{
private:
    /* data */
public:
    int shortestSubarray(std::vector<int> &nums, int k)
    {
    }
};
int main()
{
    std::vector<int> nums = {2, -1, 2};
    ShortestSubArrayAtLeastK_862 s1;
    s1.shortestSubarray(nums, 3);
    return 0;
}